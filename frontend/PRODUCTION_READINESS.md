# Production readiness checkpoint — 2026-09-20

**Not production-complete.** Installed review package: `artifacts/linux-review-20260920-lazy-filters`. Saves is excluded by user instruction. This checkpoint supersedes historical completion language in the chronological audit documents; it does not replace their evidence.

## Blocking decisions/access

1. **Native launch fix needs Windows build access.** `src/processrunner.cpp` fixes a reproduced ordinary batch launch that loses arguments and starts idle cmd.exe. The extracted C++ harness passes, but the native binary has not been rebuilt or tested. GitHub SSH authentication was subsequently verified for the requested fork pushes. The existing Windows CI workflow requires a master push or PR; a feature-branch push alone does not run it. Windows build/runtime acceptance remains outstanding. `artifacts/native-launch-windows-handoff` contains the patch, base/source hashes and build handoff. Complete runtime checks are in [NATIVE_LAUNCH_VALIDATION.md](NATIVE_LAUNCH_VALIDATION.md). Do not substitute harness success for native acceptance.
2. **Frontend framework needs clarification.** The user requested a “faithful Qt reimplementation.” The implementation is Avalonia with MO2's Qt host under Proton. It must not be described as a native Qt frontend. Asked whether Qt is required for the frontend itself or whether faithful MO2 behavior with the NMA redesign is intended. No answer yet. Further substantial Avalonia-specific redesign work should wait for this scope decision.

## Requirement/evidence review

| Requested outcome | Evidence inspected / implementation | Current conclusion |
| --- | --- | --- |
| FNV and Skyrim selectable, correct profile contents | `lazy-filters-published-roundtrip`: three passing FNV→Skyrim→FNV legs; all native filter criteria/counts, mod identities, 11/150 plugin identities, Data and Tools. Independent state-check.json reports all six state groups unchanged for both hosts. | Current package passes this bounded workflow. |
| MO2 controls mod/plugin state and load order | Current native-state comparisons above; earlier `utilities-published-plugin-drag` and mod pointer evidence exercise actual order/activation restoration. | Implemented with current readback and earlier mutation evidence; not a new mutation run on this package. |
| Mods left, Plugins right; responsive shared rows and toolbars | `lazy-filters-published-ui` and its inspected screenshot: shared row/header spacing, flag separation, rail alignment/help, category controls, 340/280/240px search. | Current package passes measured layouts. This is not full pixel-by-pixel fidelity acceptance. |
| Traditional separators, create/rename/delete dialogs, collapse/highlight | Current `Mo2SeparatorInteractionCheck` drives owned modal create/cancel/rename/delete and routed double-tap; traditional Mods source retains separators. Chronological audit records earlier native acceptance. | Implementation and historical evidence exist; this review did not rerun separator mutations. |
| Filtering remains correct across hide/reopen and game changes | Current native-filter checks close, refresh and reopen an active criterion; counts and selected control retained. Closed unfiltered panes have zero category rows until opened. | Current package passes. |
| Tabs split/join, selected state and Back/Forward | `panels-published-pointer-history/result.json`: exit 0, split/join phases, native state unchanged. Log confirms retained page instances and history targeting the moved tab. | Physical dragging and command-driven history passed on the earlier panel package. No claim of physical Back/Forward clicks here. |
| Home/game separation, Profiles navigation, sidebar and icons | Current `Mo2LiveWorkspace` routing and inspected paired screenshots; cross-game navigation passes. Earlier NMA comparison and sidebar checks are recorded in NMA_FIDELITY.md. | Implemented; visual review remains scoped, especially pending framework clarification. |
| Keyboard input works | `isolated-keyboard-20260920/check/result.json`: all ten actual XTest/xdotool phases, exit 0, native state unchanged; installed DLL hash matches. | Pass on a fresh authenticated X11 display. Real desktop still reports an active modifier; no claim that condition was fixed. |
| MO2 tools/extensions and pinned launch controls | Current roundtrip checks 3 programs/4 extension tools for FNV, 4/7 for Skyrim, launch choices and local selection retention. Earlier utilities/native extension records cover pin ordering, original INI editor and callbacks. | Current availability/navigation passes; original extension runtime evidence is historical and bounded, not universal compatibility. |
| Downloads, archives, Data, Overwrite and Steam-based External Files | Shared pages exist; External Files source has import-copy, backup cleanup and restore, superseding older detection-only audit text. Earlier artifact records cover outcome/restoration workflows; recent utility runs cover UI/lifecycle. | Implemented with mixed-age evidence. Native ordinary batch/Java activation remains blocked as above. |
| Health checks, notifications and Logs | Current implementations and earlier lifecycle/fit evidence recorded in CURRENT_STATUS.md; inspected Health audit screenshot in prior review. | Existing bounded evidence; not freshly exercised in this checkpoint. |
| Successfully load FNV with mods | FNV_ACCEPTANCE.md; `native-run-restoration.json` confirms restored profiles/test save; `fnv-current-game-order-comparison.json` reports all 14 in-game plugin indices match MO2. Actual gameplay/movement/MCM evidence is dated September 12–13. | Historical FNV gameplay acceptance exists. Not a fresh gameplay run of this package; Skyrim gameplay is not an added requirement. |
| Responsive, not laggy | Candidate readable-list times 4665/4727ms; paired baseline 7135/4708ms. Full current trace and GLX/EGL experiment retained. | Warm performance effectively unchanged. Startup consistency and broader input latency are not proven solved; do not hide the failed baseline or relax gates. |
| Reviewable installed application | Desktop launch script verified against package path; previous launcher/entry/icon backed up. Package manifest hashes all files and current evidence DLLs match. | Installed review build verified. NXM routing and open native hosts untouched. Not a production-complete release. |
| Saves | Explicitly deferred by user. | Excluded; no acceptance work requested now. |

## Next actions after replies

- With Windows access: build the exact native source patch, retain build/binary provenance, run isolated ordinary/VFS batch and mod-directory probes plus Java activation, then evaluate replacing native host binaries. Do not replace them beforehand.
- With framework scope resolved: finish the corresponding redesign review and address remaining measured responsiveness gaps. Preserve the verified MO2-owned state/workflows if the frontend framework must change.
- Keep current evidence and failed attempts. Use existing checks where they cover changed behavior; avoid re-running unchanged broad suites merely to accumulate green logs.

The remaining Windows build dependency is an external-access blocker, not a claim that the implementation is complete except for optional tests. The Qt question is a separate scope uncertainty. No source change or passing test can answer either question for the user.
